-- Portable monitor defaults.
-- Hyprland chooses the preferred mode and auto placement for each output.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})
