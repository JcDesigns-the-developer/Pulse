-- Monitor defaults. Hyprland chooses the preferred mode and automatic placement.
-- Put machine-specific monitors in ~/.config/hypr/hypr-user.lua.

hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})
